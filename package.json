{
  "private": true,
  "scripts": {
    "precommit": "lerna run precommit",
    "postinstall": "lerna bootstrap --loglevel verbose",
    "test": "lerna run test"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
